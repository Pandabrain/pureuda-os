[misc]
#skip dnf updates, this system is a bootc image
disable = ["system"]
#don't ask for confirmations
assume_yes = true
#don't ask what to do on failure
ask_retry = false
#sudo at the beginning and cache (sudo -v) to avoid blocks in between updates
pre_sudo = true