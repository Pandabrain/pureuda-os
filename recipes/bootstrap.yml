# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: files
    files:
      - source: system
        destination: /
      - source: system/etc/sudoers.d/60-silent-updates
        destination: /etc/sudoers.d/60-silent-updates
        mode: "0440"
